server:
  port: 8090
spring:
  application:
    name: hzero-generator
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    config:
      failFast: true
      retry:
        maxAttempts: 6
        multiplier: 1.5
        maxInterval: 2000
      enabled: false
management:
  port: 8091
  security:
    enabled: false



